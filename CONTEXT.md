# CMMS — Implementation Status

## What this is
A GMAO (Computerized Maintenance Management System) built as a monorepo.
Full functional spec: GMAO_Description_Fonctionnelle.pdf
Data model: data_model.pdf
Tech stack decisions: stack.pdf

## Current state
- [x] Monorepo scaffold (Turborepo + pnpm workspaces)
- [x] packages/shared — all domain enums compiled
- [x] packages/db — full Prisma schema, migration applied
- [x] Docker infrastructure — postgres, redis, minio, mailhog
- [x] apps/backend — NestJS bootstrapped:
 - [x] ConfigModule (Joi validation, fail-fast on startup)
 - [x] PrismaModule (global)
 - [x] RedisModule (global, ioredis)
 - [x] AuthModule — login/refresh/logout, JWT + refresh token rotation, Redis revocation
 - [x] AuthModule — session inactivity timeout enforced from `SystemConfig` (`SESSION_IDLE_TIMEOUT_HOURS`) across refresh JWT expiry, Redis refresh-token TTL, and refresh cookie maxAge (with 7-day fallback on invalid config)
 - [x] MailModule — BullMQ queue, Nodemailer processor, Handlebars templates (setup-account, password-reset)
 - [x] SystemConfigModule — password policy, 14 config keys seeded
 - [x] UsersModule — Admin CRUD, setup token flow, deactivate/reactivate, admin resend setup, public resend setup by email
 - [x] Auth endpoints: /auth/setup, /auth/resend-setup, /auth/forgot-password, /auth/reset-password
 - [x] StorageModule — MinIO/S3 wrapper, multi-bucket, presigned URLs
 - [x] NotificationsModule — in-app + email notifications, global
 - [x] AssetsModule — locations, categories, assets, certificates, documents
 - [x] WorkOrdersModule — state machine, assignments, intervention logs, on-hold, validation, checklist, async PDF report generation on closure, analytics cost summary, PDF cost breakdown, supervisor hold-metadata endpoint, ContractorDateOverdueJob, AccessRetryApproachingJob
- [x] AssetsModule — locations, categories, assets, certificates, documents, storage
- [x] AssetsModule — certificate expiry scheduler migrated to `@nestjs/schedule` (`@Timeout(0)` startup trigger + daily midnight `@Cron`) to replace legacy lifecycle `setInterval`
- [x] WorkOrdersModule — full state machine, assignments, intervention logs, on-hold, validation, checklist, automatic priority escalation for overdue WOs, async PDF report generation on closure
- [x] WorkOrdersModule — simultaneous maintenance authorization: `PATCH /work-orders/:id/authorize-simultaneous` (Supervisor) lifts the start-block when an asset already has an active WO; writes a status-log audit entry; notifies the principal technician via `SIMULTANEOUS_MAINTENANCE_AUTHORIZED`; supervisor detail dialog shows the action button when applicable
- [x] WorkOrdersModule — COULD_NOT_INTERVENE validation guard: `PATCH /work-orders/:id/validate` now requires `assetStatusOverride` when the last completed intervention result is `COULD_NOT_INTERVENE`; asset status is set to the supervisor's explicit choice instead of silently defaulting to OPERATIONAL; `FOLLOW_UP_PROMPT` notification dispatched to all active supervisors (not the principal technician)
- [x] WorkOrdersModule — priority escalation scoped to OPEN/ASSIGNED only: `findOverdueForEscalation` changed from `notIn [CLOSED, CANCELLED]` to `in [OPEN, ASSIGNED]`; IN_PROGRESS, ON_HOLD, and PENDING_VALIDATION WOs are no longer auto-escalated
- [x] WorkOrdersModule — CRITICAL overdue WO supervisor notification: `findOverdueCritical()` added to repository; `autoEscalateOverduePriorities()` now queries overdue CRITICAL WOs separately and dispatches `WO_OVERDUE` to all supervisors via `notifySupervisors()` instead of silently skipping them
- [x] WorkOrdersModule — cancel notifies source requester: `cancel()` reads `sourceReport.reporter.id` from the already-included Prisma relation and sends `LINKED_WO_CLOSED` to the original requester when the WO originated from a problem report
- [x] WorkOrdersModule — supervisor validation insight signals: `findById()` now enriches WO detail with `contributorsWithoutLog`, `timeDeviation`, and `hasNotableTimeDeviation` so supervisors can identify contributors without intervention logs and notable estimate-vs-actual duration deviations directly in validation context
- [x] InventoryModule — parts catalog, part requests, stock movements, low-stock alerts, analytics
- [x] InventoryModule — duplicate part `referenceCode` creates now return 409 Conflict instead of leaking Prisma unique-constraint 500s
- [x] PreventivePlansModule — plan CRUD, checklist templates, BullMQ WO generator, daily @Cron scheduler
- [x] ReportsModule — problem report lifecycle, comments, convert/reject/defer/reopen/archive
- [x] Backend integrity hardening — WO/PR reference generation now uses transaction advisory locks to prevent duplicate references under concurrent writes
- [x] Backend runtime compatibility — advisory lock SQL now casts lock keys to `int` to match PostgreSQL function overloads used in production
- [x] Backend verification — live smoke test covers auth, work orders, preventive plans, and reports
- [x] apps/web — Next.js started (auth + protected layouts + admin pages)
- [x] Auth module (web) — login page + account setup (/auth/setup) + public setup resend (/resend-setup) + password recovery (/auth/forgot-password, /auth/reset-password)
- [x] AdminModule (backend) — /admin/system-config (GET/PATCH), /admin/audit-log (GET paginated)
- [x] Admin module (web) — users management, system config panel, audit log table
- [x] Admin module (web) — hardening: ConfirmDialog for deactivate/delete, audit log filter fixed, hourlyRate clear on role change
- [x] Audit logging (backend) — comprehensive location mutations (create, update, delete) with before/after state capture
- [x] Audit logging (backend) — comprehensive category mutations (create, update, activate, deactivate) with before/after state capture
- [x] Audit logging (web) — audit log UI now exposes Location and Category audit entry types for filtering and viewing
- [x] Storekeeper module (web) — /storekeeper inventory catalog (list/filter/pagination + create/edit + activate/deactivate)
- [x] Storekeeper module (web) — stock operations on catalog rows (incoming stock, manual adjustments, per-part movement history, low-stock banner)
- [x] Storekeeper module (web) — /storekeeper/part-requests queue (list/filter/pagination + fulfill/reject dialogs)
- [x] Storekeeper module (web) — /storekeeper/analytics inventory analytics (filters + KPI cards + consumption/replenishment/dead-stock sections)
- [x] Supervisor module (web) — /supervisor/reports (list/filter/pagination + detail dialog + comment + convert/reject/defer/reopen/archive actions)
- [x] Supervisor module (web) — /supervisor/work-orders (list/filter/pagination + create WO dialog + detail dialog + full lifecycle actions: publish, assign, reassign, promote, validate, reject-closure, cancel, authorize-simultaneous-maintenance; promote rejects terminal work orders and closes the old principal intervention log when promotion happens in IN_PROGRESS; list waits for auth initialization and asset selector respects backend limits; validate panel shows a mandatory asset-status override form when the last intervention result is COULD_NOT_INTERVENE)
- [x] Supervisor module (web) — /supervisor/work-orders validation context: detail dialog now shows a validation-signals block on `PENDING_VALIDATION` WOs with contributor-without-log warnings and time-deviation indicators (estimate vs actual)
- [x] Supervisor module (web) — /supervisor dashboard summary cards now wait for auth store initialization before firing queries
- [x] Web auth compatibility — legacy redirect pages `/app/auth/setup`, `/app/auth/reset-password`, and `/app/auth/resend-setup` aligned with Next 15 `searchParams` Promise PageProps contract
- [x] Supervisor module (web) — /supervisor/preventive-plans (list/filter/pagination + create/edit + activate/deactivate + trigger-now + checklist CRUD/reorder)
- [x] Supervisor module (web) — /supervisor/assets (list/filter/pagination + create/edit + detail + status transitions)
- [x] Supervisor module (web) — asset certificates: full CRUD (add/edit/delete + optional file upload) wired in asset-detail-dialog
- [x] Supervisor module (web) — asset documents: upload + delete wired in asset-detail-dialog; document type label displayed
- [x] Storekeeper module (web) — part returns: record-return dialog with cancelled-WO picker, wired in inventory catalog
- [x] Repo hygiene — TypeScript incremental cache artifacts (`*.tsbuildinfo`) are ignored and untracked from git to keep working trees clean during local dev
- [x] Top bar / notifications (web) — balanced header layout + notification list, unread badge, mark-read actions
- [x] Admin audit (backend) — GET /admin/audit-log now accepts `actionType` query filter alongside `targetType`; admins can isolate specific action types (USER_DEACTIVATED, CONFIG_UPDATED, etc.)
- [x] Admin audit (backend) — GET /admin/audit-log now has dedicated route-level throttling (`10/min`) in addition to global throttling
- [x] Admin audit (web) — audit-log-table: `actionType` filter dropdown added (15 known action types with French labels); page resets on filter change; all hardcoded strings replaced with i18n
- [x] Admin users (web) — users-table and user-form-dialog fully converted to i18n (role labels, column headers, filters, toasts, confirm dialog, form labels)
- [x] WorkOrdersModule — daily supervisor summary email: `DailySummaryJob` (@Cron EVERY_HOUR, hour-gated via `DAILY_SUMMARY_HOUR` SystemConfig key, default 17); now collects 9 metrics in `$transaction` (adds `criticalDeferredCount` for DEFERRED reports with `deferredAt <= now - 14d`) + separate `Promise.all` for detailed lists: `overdueList` (top 10 overdue WOs), `onHoldItems` (top 10 ON_HOLD with computed `holdDurationMinutes`), `criticalDeferredItems` (top 10 oldest critical deferred reports), and `lowStockItems` (parts below threshold); mail context is role-aware per recipient (`hasStorekeeperRole`): low-stock count/list shown only when supervisor also has STOREKEEPER role; `daily-summary.hbs` updated with critical deferred section and conditional low-stock table; 53 unit tests
- [x] AdminModule (backend) — analytics endpoints: `GET /admin/analytics/users` (inactive accounts, login frequency, by-role breakdown) and `GET /admin/analytics/system` (BullMQ queue job counts for all 3 queues, failed/pending/sent notification stats); `AdminAnalyticsService` with 22 passing unit tests
- [x] Admin module (web) — analytics dashboard: `AdminAnalyticsBoard` with user-activity section (6 KPI cards, login-recency progress bars, by-role table) and system-health section (notification KPIs, one queue card per BullMQ queue); `Activity` icon nav entry in admin sidebar; `adminAnalytics` i18n section (30 + French keys)
- [x] Backend TypeScript diagnostics hygiene — `apps/backend/src/main.ts` cookie-parser import aligned with callable type usage; backend `tsconfig.json` now sets explicit `rootDir`; deprecated `baseUrl`/`moduleResolution` defaults removed where safe
- [x] NotificationsModule — Socket.io WebSocket gateway (`NotificationsGateway`) with JWT auth-on-connect, personal `user:<id>` rooms; `NotificationsService.notify()` pushes live `notification` events via gateway after persisting to DB; `IoAdapter` registered in `main.ts`; frontend `notification-menu.tsx` subscribes via `useSocket()` and refetches on receive
- [x] ReportsModule — three-tier deferred report aging: `findReportsDeferredInWindow(minHours, maxHours)` uses half-open time windows so each deferred report receives exactly one notification per tier (48h / 7d / 14d); `DeferredReportReminderJob` iterates a TIERS array; `reports-board.tsx` shows aging badge (Rappel 48h / Suivi 7j / Escalade 14j) below DEFERRED status
- [x] AssetsModule — compliance certificates soft-archived (isArchived/archivedAt/archivedById) instead of hard-deleted; migration `20260418000000_soft_archive_compliance_certificate`; all queries filter `isArchived=false`; DELETE endpoint calls `archive()` with actorId; 7 unit tests
- [x] WorkOrdersModule — duplicate active WO guard: `create()` checks for existing non-terminal WO and throws `ConflictException` with `existingWorkOrder` payload; `forceCreate?: boolean` DTO field allows supervisor override; frontend intercepts 409, shows amber warning panel with "Créer quand même" button; 12 unit tests
- [x] WorkOrdersModule — source report panel: `findById` repository query now includes `sourceReport` with reporter + description; supervisor WO detail dialog renders a source report card when applicable
- [x] WorkOrdersModule — overdue row highlighting: work orders with `dueDate < now` and non-terminal status get a red row background and "En retard" label in the supervisor board
- [x] WorkOrdersModule — WO_RESUMED notification: `resume()` emits `WO_RESUMED` to active contributor technicians (excluding the principal actor who triggered the resume); uses `notifyMany()`; 14 unit tests
- [x] WorkOrdersModule — LINKED_WO_CLOSED notification: `validate()` reads `sourceReport.reporter.id` from eagerly-loaded `findById` result and notifies the original requester on every WO closure that originated from a problem report; also fires on the CNI path
- [x] WorkOrdersModule — DueDateApproachingJob (`@Cron(EVERY_HOUR)`): queries WOs with `dueDate` in next 24h and active statuses; 23h dedup window prevents hourly re-notification for the same WO; notifies `principalTechnicianId` with `DUE_DATE_APPROACHING`; registered in `WorkOrdersModule`; 15 unit tests
- [x] WorkOrdersModule — ValidationReminderJob (`@Cron(EVERY_HOUR)`): queries WOs stuck in `PENDING_VALIDATION` for at least 24h (`updatedAt <= now - 24h`); 23h dedup window prevents hourly re-notification for the same WO; notifies supervisors with `VALIDATION_REMINDER_24H`; registered in `WorkOrdersModule`; 8 unit tests
- [x] WorkOrdersModule — hold actor fix: `resume()` no longer writes `supervisorResolutionNote` (was wrong actor); `ResolveHoldDto` `resolutionNote` field removed; new `PATCH /work-orders/:id/hold-metadata` (Supervisor only) via `UpdateHoldMetadataDto` sets `expectedResolutionDate`, `retryDate`, `resolutionNote` on the active `OnHoldPeriod`; `updateHoldMetadata()` validates WO is ON_HOLD, finds most-recent unresolved period, applies partial update; empty DTO is a safe no-op; 8 new unit tests in `on-hold.service.spec.ts`
- [x] WorkOrdersModule — ContractorDateOverdueJob + AccessRetryApproachingJob (`@Cron(EVERY_HOUR)`): `ContractorDateOverdueJob` fires `CONTRACTOR_DATE_OVERDUE` to supervisors when `EXTERNAL_CONTRACTOR` hold's `expectedResolutionDate` is in the past; `AccessRetryApproachingJob` fires `ACCESS_RETRY_APPROACHING` to supervisors when `ACCESS_DENIED` hold's `retryDate` is within 24h; both use 23h dedup windows; 8 unit tests each
- [x] Supervisor module (web) — hold management UI: `WorkOrderOnHoldPeriod` type fixed (`reasonType`/`detail` vs wrong `reason`/`note`; added `expectedResolutionDate`, `retryDate`, `supervisorAssetStatusChoice`, `supervisorResolutionNote`); hold period cards render all fields; when WO is ON_HOLD a collapsible supervisor form allows setting `expectedResolutionDate`, `retryDate`, `resolutionNote` via `PATCH /hold-metadata`; all new strings i18n'd
- [x] Notifications (web) — deep-linking: `notification-menu.tsx` resolves `entityType + roles[]` to a URL via `resolveNotificationRoute` (`lib/notification-routing.ts`); clicking a notification navigates and closes the dropdown; `work-orders-board.tsx` reads `?id=` via `useSearchParams` and auto-opens the detail dialog; `supervisor/work-orders/page.tsx` wraps the board in `<Suspense>` (required by Next.js 15)
- [x] apps/web — Jest test infrastructure: `jest` + `ts-jest` + `@types/jest` devDependencies; `moduleNameMapper` for `@/` path alias and `@gmao/shared`; 17 unit tests for `notification-routing.ts`
- [x] WorkOrdersModule — cancellation contract guard: cancelling with `EXTERNAL_DECISION` or `RESOLVED_OTHERWISE` now requires non-blank `detail` at DTO and service levels (`workOrders.cancellationDetailRequired`); cancellation detail is trimmed before persistence; backend unit + controller integration coverage added
- [x] WorkOrdersModule — promotion guard: `promote()` now rejects terminal work orders via the shared state-machine guard and closes the old principal's open intervention log when the work order is already `IN_PROGRESS`; backend unit + controller integration coverage added
- [x] WorkOrdersModule — caller-supplied promote reason: `PromoteTechnicianDto` gains optional `reason?: WOReassignmentReason` and `reasonDetail?: string`; `assignmentService.promoteTechnician()` passes caller values to the reassignment log instead of hardcoding `TECHNICIAN_ABSENT`; 2 new unit tests
- [x] WorkOrdersModule — isReassignmentRemnant on intervention logs: `WorkOrderInterventionLog` frontend type gains `isReassignmentRemnant: boolean`; WO detail dialog renders an amber pill "incomplet — réassigné" label for force-closed sessions
- [x] WorkOrdersModule — technician queue filter in supervisor board: `WorkOrderListQuery` frontend type gains optional `technicianId`; supervisor dashboard technician-load rows are now Next.js Links to `/supervisor/work-orders?technicianId=<id>`; `WorkOrdersBoard` reads `technicianId` from URL search params, shows a dismissible amber filter chip, and passes the param through to `workOrdersApi.list()`; backend `WorkOrderQueryDto` already supported this param — no backend changes required; `technician-filter.spec.ts` (4 tests) + `work-orders.api.spec.ts` updated
- [x] ReportsModule — report detail enriched with asset context: `ReportsRepository.findById()` now includes `asset.workOrders` (non-terminal), `asset.certificates` (EXPIRING_SOON/EXPIRED, non-archived), and `assetInterventionHistory` (last 5 closed WOs via separate query to avoid Prisma relation-filter limitation); 7 unit tests in `reports.repository.spec.ts`
- [x] Supervisor module (web) — report detail asset context and duplicate WO warning: `reports-board.tsx` shows an amber duplicate-WO banner when `report.asset.workOrders` is non-empty, and an asset context sidebar card with certificate alerts (EXPIRING_SOON/EXPIRED badges) and intervention history; `ReportDetailItem` frontend type gains `asset.workOrders`, `asset.certificates`, and `assetInterventionHistory`; three new interfaces: `ReportAssetActiveWO`, `ReportAssetCertAlert`, `ReportAssetInterventionHistoryItem`; `reports-detail-enrichment.spec.ts` (5 tests)
- [x] WorkOrdersModule — checklist anomaly-WO source attribution fix: `WorkOrderSource.CHECKLIST_ANOMALY` added to Prisma schema, `@gmao/shared` enum, and migration `20260420143130_add_checklist_anomaly_source`; `ChecklistService.completeItem()` now creates auto-corrective WOs with `sourceType: CHECKLIST_ANOMALY` (not `PREVENTIVE_PLAN`) and propagates `sourcePlanId` from the parent WO — enabling correct analytics separation between plan-generated WOs and anomaly-triggered correctives; `@gmao/db` rebuilt; 12 unit tests
- [x] WorkOrdersModule — cost summary on WO detail endpoint: `GET /work-orders/:id` now computes and returns `costSummary: { laborCost, partsCost, contractorCost, totalCost }` via `calculateWorkOrderCostSummary` applied inline in the controller; 2 new integration tests covering zero-cost and non-zero-cost cases
- [x] Supervisor module (web) — checklist display fix + cost summary section: removed the ghost `completedNote` field from `WorkOrderChecklistItem` frontend type and replaced it with the actual DB fields `anomalyDescription` and `notApplicableReason`; fixed checklist status badge variants (was checking non-existent `COMPLETED`/`SKIPPED`, now correctly handles `DONE`/`ANOMALY_DETECTED`/`NOT_APPLICABLE`/`PENDING` via `checklistStatus.*` i18n map); checklist items now display `anomalyDescription` and `notApplicableReason` sub-text inline; WO detail dialog includes a cost summary section rendering `laborCost`, `partsCost`, `contractorCost`, and `totalCost` from the `costSummary` field; `WorkOrderDetail` and `WorkOrderAnalyticsResponse` frontend types updated; i18n keys added for checklist status labels and cost breakdown
- [x] JobLoggerModule — cross-cutting observability module (`apps/backend/src/job-logger/`): `JobLoggerService` upserts a `ScheduledJobLog` record (`jobName UNIQUE`, `lastRunAt`, `lastSuccessAt`, `lastFailureAt`, `lastErrorMessage`); all 6 cron jobs wrapped with try/catch calling `recordStart`/`recordSuccess`/`recordFailure`; DB errors in logger are swallowed so they never interrupt job execution; `JobLoggerModule` imported by both `WorkOrdersModule` and `AdminModule`; migration `20260421000000_scheduled_job_log`; 9 unit tests; 328 backend tests total
- [x] AdminModule (backend) — scheduled job health in analytics: `getSystemHealthStats()` now fetches `scheduledJobs: ScheduledJobStatus[]` via `JobLoggerService.getAll()` in parallel with queue/notification queries and includes them in the `SystemHealthStats` response; admin analytics board renders a "Tâches planifiées" section with per-job status badge (healthy/failed/unknown), timestamps, and last error message; 2 new `admin-analytics.service.spec.ts` tests
- [x] Supervisor module (web) — QR code rendering and print: `asset-detail-dialog.tsx` renders a `<QRCode>` SVG (react-qr-code) beside the identifier and a print button; `lib/qr-print.ts` provides `buildQrPrintHtml()` (XSS-safe standalone print document) and `openQrPrintWindow()` (popup + auto-print); 10 `qr-print.spec.ts` tests covering XSS escaping, SVG injection, HTML structure, and print trigger; 27 frontend tests total
- [x] WorkOrdersModule — `closedAfter`/`closedBefore` date filters on `GET /work-orders`: `WorkOrderQueryDto` gains two optional `@IsDateString()` params; `WorkOrdersRepository.findAll` translates them to `closedAt: { gte, lte }` Prisma predicate; absent params produce no `closedAt` clause so all existing callers are unaffected; 8 unit tests in new `work-orders.repository.spec.ts` (341 backend tests total)
- [x] AssetsModule — certificate alerts endpoint: `CertificatesService.findAlerts()` queries all non-archived `EXPIRING_SOON`/`EXPIRED` certs with their parent asset name, ordered by expiration date ascending; new `GET /assets/certificates/alerts` route (Supervisor only, placed before `/:id` to avoid routing conflict); `CertificateAlertItem` interface exported from the service; 5 unit tests added to `certificates.service.spec.ts`
- [x] Supervisor module (web) — dashboard operational panels: three new panels below the existing summary cards — (1) "Clôturés aujourd'hui" queries `GET /work-orders?status=CLOSED&closedAfter=<UTC-midnight>`; (2) "Demandes bloquées" fetches PENDING part requests and counts those on `ON_HOLD` WOs with amber border when non-zero; (3) "Certificats à risque" renders a live list (up to 4 rows) from `GET /assets/certificates/alerts` with EXPIRED/EXPIRING_SOON badges; pending-validation card CTA now links to `/supervisor/validation-queue`; `lib/date-utils.ts` extracted with `elapsedSince()` and `todayStartIso()` pure utilities; `CertificateAlertItem` type and `assetsApi.getCertificateAlerts()` added to `assets.api.ts`; `closedAfter`/`closedBefore` params added to `WorkOrderListQuery`; 10 `date-utils.spec.ts` tests (37 frontend tests total)
- [x] Supervisor module (web) — validation queue dedicated view: new `/supervisor/validation-queue` page with `ValidationQueueBoard` component; table shows all `PENDING_VALIDATION` WOs with columns: reference, asset+location, technician, type badge, priority badge, time-in-queue (`elapsedSince`); pagination; opens existing `WorkOrderDetailDialog` on row click for approve/reject; "File de validation" nav item added to supervisor sidebar (ShieldCheck icon); `nav.validationQueue`, `validationQueue.*` keys added to `common.json`
- [x] WorkOrdersModule — COULD_NOT_INTERVENE follow-up WO creation: `WorkOrderSource.FOLLOW_UP` added to Prisma schema + `@gmao/shared` enum + migration `20260422000001_add_follow_up_source`; `CreateFollowUpDto` in backend; `WorkOrdersService.createFollowUp()` validates original WO is CLOSED, inherits `assetId` from original, creates new WO in DRAFT with `sourceType=FOLLOW_UP` and `followUpFromId`; `POST /work-orders/:id/follow-up` (Supervisor only); `repository.create()` extended with optional `followUpFromId`; `findById` includes `followUpFrom`/`followUps` self-relations; 9 backend unit tests
- [x] Supervisor module (web) — follow-up WO prompt after COULD_NOT_INTERVENE validation: after a successful CNI validation, the detail dialog captures pre-mutation context in a `useRef` and presents an amber prompt panel offering to create a linked follow-up WO; `createFollowUp` mutation calls `POST /work-orders/:id/follow-up`; WO detail view shows cross-reference section with `followUpFrom`/`followUps` links; `WorkOrderCrossRef` type + `createFollowUp`/`getTechnicianLoad` API methods added to `work-orders.api.ts`; `supervisorWorkOrders.followUp.*` and toast i18n keys added; 10 frontend unit tests in `follow-up-utils.spec.ts`
- [x] WorkOrdersModule — technician load endpoint: `WorkOrdersService.getTechnicianLoad()` aggregates active assignments on non-terminal WOs, counting `openWoCount` and flagging `hasCritical` if any assignment is on a CRITICAL WO; `GET /work-orders/technician-load` (Supervisor only, declared before `/:id` to prevent route shadowing); `TechnicianLoadItem` interface exported; 5 backend unit tests
- [x] Supervisor module (web) — technician load panel on dashboard: supervisor dashboard gains a panel showing per-technician open WO count with a CRITICAL badge indicator; data from `getTechnicianLoad` query; sorted descending by count; `supervisorDashboard.technicianLoad.*` i18n keys added; 350 backend + 47 frontend tests total (0 regressions)
- [x] NotificationsModule — `notifyAdmins()` method added: symmetric to `notifySupervisors()`, queries active ADMIN users and fans out via `notifyMany()`; 3 unit tests
- [x] JobLoggerService — admin alert on job failure: `recordFailure()` now injects `@Optional() NotificationsService` and emits `SCHEDULED_JOB_FAILED` to all active admins after persisting the failure log; 23h dedup per job name prevents hourly spam for persistent failures (`entityType='ScheduledJob'`, `entityId=jobName`); `@Optional()` injection preserves full backward compatibility with existing unit tests that construct the service without notifications; all message text French
- [x] AdminModule — `FailedNotificationDetectorJob`: new `@Cron(EVERY_HOUR)` job in `apps/backend/src/admin/`; counts `Notification` rows with `emailFailed: true` created in the last 23h; if count > 0 and no recent `NOTIFICATION_DELIVERY_FAILED` dedup entry (`entityType='system'`, `entityId='email-delivery'`), emits `NOTIFICATION_DELIVERY_FAILED` to all active admins including count in the summary; registered in `AdminModule`; wraps execution in `jobLogger` calls for observability; 15 unit tests; 367 backend tests total (0 regressions)
- [x] AssetsModule — document versioning: `DocumentsService._doUpload()` implements automatic version archiving on every upload — finds any `isCurrentVersion: true` doc for the same `entityType+entityId+documentType`, marks it `isCurrentVersion=false` + `replacedById=newDocId` inside a `$transaction`, creates new doc with `version+1`; first upload creates `version=1`; `getVersionHistory(docId)` returns the full chain ordered by `version desc`; asset, part, and plan documents all benefit from the same logic
- [x] InventoryModule — part catalog document attachments: `DocumentsService.findByPart()` + `uploadForPart()` with allowed-type guard (TECHNICAL_MANUAL, SAFETY_DATA_SHEET, SPECIFICATION_SHEET); `AssetsModule` now exported from itself and imported by `InventoryModule`; `PartsController` gains 5 document endpoints: `GET/POST :id/documents`, `GET :id/documents/:docId/download`, `GET :id/documents/:docId/versions`, `DELETE :id/documents/:docId` — write roles: SUPERVISOR + STOREKEEPER; `PartDocumentsDialog` component added to storekeeper inventory catalog (FileText icon per part row); `inventoryApi` gains `listPartDocuments`, `uploadPartDocument`, `getPartDocumentDownloadUrl`, `getPartDocumentVersionHistory`, `deletePartDocument`; `storekeeperInventory.documents.*` i18n keys
- [x] PreventivePlansModule — plan document attachments: `DocumentsService.findByPlan()` + `uploadForPlan()` with allowed-type guard (PROCEDURE_DOCUMENT, SAFETY_DATA_SHEET, SPECIFICATION_SHEET); `PreventivePlansModule` imports `AssetsModule`; `PreventivePlansController` gains 5 document endpoints — write role: SUPERVISOR only; inline documents section added to `PreventivePlanDetailDialog` above the checklist; `preventivePlansApi` gains `listPlanDocuments`, `uploadPlanDocument`, `getPlanDocumentDownloadUrl`, `getPlanDocumentVersionHistory`, `deletePlanDocument`; `supervisorPreventivePlans.documents.*` i18n keys; 408 backend + 74 frontend tests (0 regressions)
- [x] InventoryModule — storekeeper cost analytics and long-waiting request detection: `InventoryRepository.getCostTrend(periodDays)` groups OUTGOING movements by month using raw SQL (`COALESCE(unitCostAtTime, unitCost, 0)`); `InventoryRepository.getLongWaitingOnHoldRequests(thresholdHours)` finds PENDING part requests on ON_HOLD WOs waiting beyond the threshold; `InventoryService.getAnalytics()` extended with `longWaitingThresholdHours=24` param; `GET /stock/analytics` accepts `?longWaitingThresholdHours`; `StockAnalyticsBoard` adds "Évolution des dépenses" table + "Demandes en attente sur OT bloqués" amber-badged section; `InventoryAnalyticsResponse` extended; `InventoryCostTrendItem` + `LongWaitingPartRequest` types added; i18n keys in `storekeeperAnalytics.*`; 9 new backend tests (417 total, 0 regressions)
- [x] InventoryModule — parts consumption breakdown by asset category and WO type: `InventoryRepository.getConsumptionBreakdown(periodDays)` raw SQL joins `StockMovement → WorkOrder → Asset → AssetCategory`, groups by `partId × categoryId × wo.type`, restricted to top-20 parts by outgoing quantity via inline subquery; post-query JS builds nested `PartConsumptionBreakdown[]` (part → `byAssetCategory[]` → `byWoType[]`); movements with no WO appear under `categoryId: null / woType: null`; `InventoryService.getAnalytics()` runs it in parallel and exposes `consumptionBreakdown` field; `stock-analytics-board.tsx` renders a per-part accordion Card with category × WO-type table and coloured `Badge` labels; `ConsumptionBreakdownWoTypeEntry`, `ConsumptionBreakdownCategoryEntry`, `PartConsumptionBreakdown` interfaces added to `inventory.api.ts`; 7 new backend tests (19 total, 0 regressions)
- [x] WorkOrdersModule — requester analytics: report accuracy rate and duplicate submission rate: `ProblemReport` gains `submittedDespiteWarning Boolean @default(false)` (migration `20260425145202`); `CreateReportDto` gains optional `submittedDespiteWarning?: boolean` (`@IsOptional @IsBoolean`); `ReportsRepository.create()` persists the field; `getAnalytics()` reportsInPeriod query extended to select `submittedDespiteWarning` and `derivedWorkOrders.{status, interventionLogs.result}`; `reportAccuracyRate` computed as resolved-closed-conversions / total-closed-conversions (null when denominator is zero); `duplicateSubmissionRate` computed as submittedDespiteWarning-count / total-reports (null when no reports); both returned in `requesterAnalytics` object; `SupervisorAnalyticsBoard` requester tab gains two new `KpiCard` components; 4 i18n keys added; 10 new unit tests covering all null/zero/fraction/combined cases (0 regressions)
- [x] WorkOrdersModule — technician pre-assignment and duration hints on WO creation: `CreateWorkOrderDto` gains `principalTechnicianId?` and `contributorIds?[]`; `WorkOrdersController.create()` becomes async — if `principalTechnicianId` is provided, calls `workOrders.publish()` then `assignment.assign()` inside the same request; `GET /work-orders/duration-hints?assetId&type&technicianId` returns `{ last5AssetAvgDays, categoryAvgDays, technicianAvgDays }` (all nullable) computed via `Promise.all` with 3 conditional queries rounded to 1 decimal; route declared before `/:id` to prevent shadowing; `DurationHintsQueryDto` added with `@IsEnum(WorkOrderType)` validator; frontend `work-order-form-dialog.tsx` rewritten: technician select (shows open-WO load + CRITICAL badge), contributor checkboxes, duration-hints blue panel with 3-column dl; `usersApi.listTechnicians()` + `workOrdersApi.getDurationHints()` API calls added; `DurationHintsResponse` interface + `TechnicianOption` interface added; i18n keys `supervisorWorkOrders.form.{principalTechnician,technicianPlaceholder,contributors,techLoad,techNoLoad,durationHints.*}`; `GET /users/technicians` added with SUPERVISOR+ADMIN access (method-level `@Roles` override); 6 new backend tests (423 total, 0 regressions)
- [x] UsersModule — per-user email notification preferences: `UsersService.getPreferences(userId)` → `{ emailNotificationsEnabled }` (NotFoundException if missing); `UsersService.updateEmailNotificationsPreference(userId, enabled)` → returns updated value; `UpdateEmailNotificationsDto` with `@IsBoolean() enabled`; `GET /users/me/preferences` + `PATCH /users/me/email-notifications` endpoints with all-roles `@Roles` override; `app-sidebar.tsx` sidebar dropdown gains email notification toggle menu item — reads current preference via `useQuery`, toggles via `useMutation`, optimistic cache update on success; `usersApi.getMyPreferences()` + `usersApi.updateEmailNotifications()` added; i18n keys `userPreferences.emailNotifications.*`; 11 new backend tests (434 total, 0 regressions)
- [x] PreventivePlansModule — same-asset same-day conflict detection and supervisor notification: `PreventivePlansRepository.findSameDayAssetConflicts()` groups due plans by `assetId` and returns `Map<assetId, conflictDetails[]>` (`{ planId, planTitle, assetName }`) for any asset with 2+ plans due the same day; `PlanSchedulerService.scheduleDuePlans()` calls this before enqueueing WO generation jobs and dispatches a supervisor notification ("Conflit détecté : plusieurs plans préventifs") including asset name, conflict count, and plan titles when conflicts are found; notification entity type is `Asset` for deep-linking; all conflicting plans are still enqueued — notification is informational only; `NotificationsModule` imported by `PreventivePlansModule`; `AssetSummary` + `TechnicianSummary` helper types added; `PlanWithRelations` extended with `asset` + `defaultTechnician`; 12 unit tests (6 repository + 6 scheduler), 507 backend total, 0 regressions
- [x] PreventivePlansModule — foreseeable WO generation calendar preview: `PreventivePlansService.getCalendarPreview(fromDate, toDate)` projects upcoming WO generation dates by looping `computeNextDueAt()` with a `MAX_ITEMS_PER_PLAN=200` safety cap; returns `CalendarPreviewItem[]` sorted by `generationDate` ASC; `GET /preventive-plans/calendar?fromDate&toDate` (SUPERVISOR only, inline `CalendarQueryDto`, defaults to today + 90 days, declared before `/:id`); `CalendarPreviewItem` interface exported; frontend `preventive-plans-board.tsx` gains a List/Calendar toggle — calendar view groups items by day, shows plan title + asset + technician name + estimated duration; `preventivePlansApi.getCalendar()` API call + `CalendarPreviewItem` interface added; i18n keys `supervisorPreventivePlans.{views.*,calendar.*}`; 8 new backend tests (442 total, 0 regressions)
- [x] WorkOrdersModule — full KPI analytics and asset health recurring-failure detection: `getAnalytics(periodDays, categoryId?)` extended with 5 new KPI categories computed in a single `Promise.all`: asset KPIs (global MTBF/MTTR, top-10 failing assets, top-10 cost assets, preventive compliance rate); technician KPIs (closed WO count, first-pass rate, avg active duration, avg response time, avg hold periods per WO); requester analytics (conversion rate, avg report-to-action delay); preventive plan efficiency (compliance rate, anomaly rate); operational overview (source distribution, rejection reasons, reassignment count); `GET /work-orders/analytics` gains optional `?categoryId`; `GET /work-orders/asset-health?thresholdCount=3&periodDays=90` returns assets breaching corrective-failure threshold (Supervisor only); `SupervisorAnalyticsBoard` rewritten with 6-tab navigation (no external tab library); asset health panel added to supervisor dashboard between technician load and operational alerts; `AssetHealthItem` + `TechnicianKpiItem` + `AssetFailureKpiItem` + `AssetCostKpiItem` interfaces; `supervisorAnalytics.{tabs,kpi,sections,columns,states.noData}` and `supervisorDashboard.assetHealth.*` i18n keys; 4 new backend tests + 6 frontend tests (441 backend + 87 frontend total, 0 regressions)
- [x] WorkOrdersModule — technician KPI rejection rate by category: `techKpiWOs` Prisma select extended to include `rejectionReason` on `validationActions`; `techMap` accumulates `rejectionsByReason: Map<string,number>` per technician (null reasons silently skipped); each technician KPI output gains `rejectionCount` (total REJECTED actions), `rejectionRate` (rejectionCount/closedCount, 3dp, null when closedCount=0), and `rejectionRateByCategory: Record<ValidationRejectionReason, {count,rate}>`; `TechnicianRejectionCategoryEntry` + `TechnicianKpiItem` frontend types extended; `TechRow` gains 7th column "Taux de rejet" (destructive red when > 0) + muted sub-row with per-reason inline tags using `validationRejectionReason.*` i18n keys; "Détail des rejets par technicien" card added to technician tab; table min-w raised to 700px; 4 i18n keys added (`supervisorAnalytics.columns.{rejectionRate,rejectionCount}`, `supervisorAnalytics.sections.{technicianRejectionBreakdown,technicianRejectionBreakdownDesc}`); 5 backend unit tests + 3 frontend API tests (495 backend + 120 frontend total, 0 regressions); also fixed pre-existing mock regression: all 8 `beforeEach` blocks in `work-orders.service.spec.ts` were missing `StorageService` + `ReportGenerationService` providers (added both + imports); pdfkit CJS mock fixed in both `report-generation.service.spec.ts` and `report-generation.e2e.spec.ts` (factory was returning `{__esModule:true, default:Class}` instead of class directly, causing `TypeError: PDFDocument is not a constructor` under `module: commonjs`)
- [x] WorkOrdersModule — on-demand PDF report download for closed WOs: `ReportGenerationService` pdfkit import fixed (`import * as PDFDocument` — CJS interop under `module: commonjs`); `WorkOrdersService.getReportUrl(id)` generates PDF on first request when `reportPdfKey` is null (on-demand fallback for WOs closed before BullMQ job completed), uploads to `pdfs` bucket, persists key, returns presigned URL; `GET /work-orders/:id/report` (Supervisor only) added to controller as a clean delegate; `WorkOrderDetail` frontend type gains `reportPdfKey: string | null`; download button rendered in closed-WO dialog footer (spinner while in-flight, opens presigned URL in new tab, toast on error); i18n keys `supervisorWorkOrders.actions.{downloadReport,reportNotReady}` + `supervisorWorkOrders.toasts.reportDownloadError`
- [x] WorkOrdersModule — isOverdue server-side filter: `WorkOrderQueryDto` gains `isOverdue?: boolean` (Transform coerces `"true"` string from query params); `WorkOrdersRepository.findAll()` applies `dueDate: { not: null, lt: new Date() }` + `status: { in: NON_TERMINAL }` when `isOverdue=true`; `WorkOrderListQuery` frontend type extended; `workOrdersApi.list()` passes the param; `work-orders-board.tsx` reads `?isOverdue=true` from URL search params and pre-filters the list; supervisor dashboard `page.tsx` adds a dedicated overdue panel with `OverdueWorkOrderRow` (reference, asset name, days-overdue badge, direct link to WO detail via `?id=`), overflow note for > 5 results, and "Voir tous les OT en retard" link to `/supervisor/work-orders?isOverdue=true`; i18n keys `supervisorDashboard.overduePanel.*`; 8 backend repository unit tests + 6 controller integration tests + 13 frontend logic tests (0 regressions)
- [x] WorkOrdersModule — isActive server-side filter: `WorkOrderQueryDto` gains `isActive?: boolean` (same Transform pattern as `isOverdue`); `WorkOrdersRepository.findAll()` applies `status: { in: [ASSIGNED, IN_PROGRESS, ON_HOLD] }` when `isActive=true`; `WorkOrderListQuery` frontend type extended; `WorkOrdersBoard` reads `?isActive=true` from URL search params, passes it through to `workOrdersApi.list()`, and shows a dismissible filter badge; the board also reads `?status=<WorkOrderStatus>` on mount to pre-select the status dropdown from any direct link; supervisor dashboard "OT actifs" card links to `/supervisor/work-orders?isActive=true`; "Clôturés aujourd'hui" panel upgraded from count-only to an inline list (`ClosedTodayRow` — asset name, technician name, WO type badge; limit raised to 5; overflow note; CTA links to `?status=CLOSED`); i18n keys `supervisorWorkOrders.filters.{activeFilter, clearActiveFilter}` and `supervisorDashboard.operational.closedToday.{none, more, viewWo}`; 9 backend unit tests + 19 frontend logic tests (0 regressions)
- [x] Storekeeper module (web) — /storekeeper/low-stock dedicated view: `LowStockView` component queries `GET /stock/low` (returns all parts below minimum threshold, ordered by deficit on the backend); renders a sortable table with columns Pièce, Référence, Emplacement, Stock actuel, Seuil min., Déficit; deficit column computed as `minimumStockThreshold − currentStock`; client-side sort by deficit/name/currentStock with direction toggle; each row has a "Réceptionner" button opening `StockIncomingDialog`; loading/empty/error states; "Stock bas" nav item (AlertTriangle) added between Inventaire and Demandes de pièces; pure logic in `lib/low-stock-utils.ts` (`computeDeficit`, `sortLowStockParts`, `toggleSortDir`); `storekeeperLowStock.*` i18n section; 15 unit tests (164 frontend total, 0 regressions)
- [x] Admin module (web) — system config panel rewritten: all 14 known keys (4 password-policy + 10 operational) organised into 6 named Card sections with French labels and descriptions from i18n; all non-boolean keys render `type="number"` inputs with `min`/`max` from `SYSTEM_CONFIG_KEY_CONSTRAINTS`; `useTranslation` replaces all hardcoded strings; logic extracted to `lib/system-config-groups.ts` (`SYSTEM_CONFIG_GROUPS`, `SYSTEM_CONFIG_BOOLEAN_KEYS`, `SYSTEM_CONFIG_KEY_CONSTRAINTS`, `ALL_KNOWN_KEYS`); 38 new `admin.systemConfig.*` i18n keys; 12 unit tests (164 frontend total, 0 regressions)
- [x] WorkOrdersModule / Storekeeper analytics (web) — type fix: `WorkOrderAnalyticsResponse.requesterAnalytics` frontend interface extended with `reportAccuracyRate: number | null` and `duplicateSubmissionRate: number | null` to match the backend response shape; `analytics.spec.ts` fixtures updated with missing `TechnicianKpiItem` rejection fields and `requesterAnalytics` new fields
- [x] AssetsModule — configurable location hierarchy level names: `LocationsService.getLevelNames()` reads `SystemConfig` keys `LOCATION_LEVEL_1_NAME` through `LOCATION_LEVEL_5_NAME` (French defaults: Bâtiment/Étage/Zone/Salle/Sous-zone for levels 1–5) via `Promise.all`; `setLevelNames(dto, actorId)` writes only the levels present in `dto.items` — other levels retain their current stored values; `UpdateLevelNamesDto` with `@ValidateNested` items array; `GET /locations/level-names` (all roles, declared before `/:id`) + `PATCH /locations/level-names` (Admin only); `LevelNamesCard` admin UI component shows read-mode badges + inline edit form per level; `LocationsTable` resolves per-row level badge from live config; `locationsApi.getLevelNames()`/`setLevelNames()` added; i18n keys `admin.locations.levelNames.*`; 7 backend unit tests + 4 frontend API tests (536 backend / 124 frontend total, 0 regressions)
- [ ] apps/mobile — Expo (not started)

## Key file locations
- Prisma schema: packages/db/prisma/schema.prisma
- Applied migrations: packages/db/prisma/migrations/
- Shared enums: packages/shared/src/enums/
- Infrastructure: docker-compose.yml
- Env template: .env.example
- Backend source: apps/backend/src/
